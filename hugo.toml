baseURL = 'https://example.test/'
languageCode = 'en'
title = 'An open experiment'
disableKinds = ['taxonomy', 'term', 'RSS', 'sitemap']
[params]
  description = 'Small experiments. Notes along the way.'
  disclosure = 'Written and built with AI. An experiment in making things and learning what works.'
[markup]
  [markup.goldmark]
    [markup.goldmark.parser]
      wrapStandAloneImageWithinParagraph = false
